<div class="container my-3">
  <h1 class="text-primary mb-3">Danh sách sản phẩm</h1>
  <a href="/add"><button class="btn btn-success mb-3">Thêm sản phẩm</button></a>
  <p>Chọn một sản phẩm cụ thể để xem chi tiết</p>
  <table class="table table-striped table-hover">
    <thead>
      <tr>
        <th>STT</th>
        <th>Tên sản phẩm</th>
        <th>Giá</th>
        <th>Thao tác</th>
      </tr>
    </thead>
    <tbody>
      {{#each products}}
        <tr>
          <td>{{id}}</td>
          <td>{{name}}</td>
          <td>${{price}}</td>
          <td>
            <a href="/edit/{{id}}" class="btn btn-sm btn-primary">Chỉnh sửa</a>
            <a href="/{{id}}" class="btn btn-sm btn-warning">Chi tiết</a>
            <a href="/delete/{{id}}" class="btn btn-sm btn-danger">Xóa</a>
          </td>
        </tr>
      {{/each}}
    </tbody>
  </table>
</div>
<div class="container my-5">
  <div class="row">
    <div class="col-6" style="background-size:cover"><img
        src="/{{img}}"
        alt="img"
        style="width:auto;height:450"
      /></div>
    <div class="col-6">
      <h1>{{name}}</h1>
      <p>${{price}}</p>
      <br />
      <p>{{desc}}</p>
      <button class="btn btn-success">Buy Now</button>
      <button class="btn btn-primary">Add to Cart</button>
    </div>
  </div>
</div>